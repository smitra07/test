---
- name: copy script
- hosts: source_system
  tasks:
  - name: transfer data from machine1 to machine2

    copy:
     src: /root/scripts/loadtest.sh
     dest: /root/scripts/

    delegate_to: deployee_system
